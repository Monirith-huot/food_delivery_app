//Where all the defaults color store
import 'package:flutter/material.dart';

class SIZE {
  static const iconsSize = 14.0;
  static const headerTextSize = 32.0;
  static const titleTextSize = 24.0;
  static const textSize = 16.0;
  static const subTextSize = 12.0;
  static const smallTextSize = 8.0;
  static const buttonHeight = 56.0;
  static const radius = 10.0;

  static const smallButtonWidth = 170.0;
  static const medimunButtonWidth = 300.0;
  static const bigButtonHeight = 370.0;
  static const bigPadding = 30.0;
  static const smallPadding = 10.0;
}

class COLORS {
  static const primary = Color.fromRGBO(247, 122, 54, 1);
  static const white = Color.fromRGBO(255, 255, 255, 1);
  static const grey = Color.fromRGBO(196, 196, 196, 1);
  static const black = Color.fromRGBO(34, 34, 34, 1);
  static const red = Color.fromRGBO(238, 92, 92, 1);
  static const green = Color.fromRGBO(2, 184, 117, 1);
}
